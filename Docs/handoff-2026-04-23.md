# 세컨드브레인 핸드오프 문서
**작성일**: 2026-04-23  
**세션**: 컨텍스트 컴팩션 이후 재개 세션

---

## 세션 로그

| 구분 | 세션 ID |
|------|---------|
| 현재 세션 | `425a1944-30d3-4d81-a473-a2bea498bd82` |
| 전체 기록 | `C:\Users\kjswi\.claude\projects\C--KJS-AVARTA-secondBrain\425a1944-30d3-4d81-a473-a2bea498bd82.jsonl` |

---

## 이번 세션에서 구현한 내용

### 1. Notion 동기화 완성 (P5)

- **Notion Integration 키 등록**: `ntn_127142...` → `.env`의 `NOTION_API_KEY` 업데이트
- **데이터베이스 자동 탐색**: API 조회로 기존 `SecondBrain Vault` DB 발견 (`33e2f5d3-8f22-8131-b2df-c0f9c444a1fd`)
- **`NOTION_DATABASE_ID` 자동 저장**: `.env` 업데이트
- **`sync-notion.py` 버그 수정**: `NOTION_TOKEN` → `NOTION_API_KEY` 환경변수 이름 통일
- **동기화 실행 결과**: `01-literature/` 14개 노트 → Notion 전송 성공 (14/14)
- **n8n 파이프라인 통합**: `Auto-Link Notes → Sync to Notion → Notify: Linear` 순서로 노드 삽입
- **docker-compose.yml**: n8n 서비스에 `NOTION_API_KEY`, `NOTION_DATABASE_ID` 환경변수 추가

---

### 2. GPT Researcher 구동 (P4 확장)

- **docker-compose.yml 추가**: `gptresearcher/gpt-researcher:latest` 기반 서비스 (포트 8888)
- **DeepSeek V3 연결**: `OPENAI_BASE_URL=https://api.deepseek.com/v1`, `OPENAI_API_KEY=${DEEPSEEK_API_KEY}`
- **오류 수정 1**: `FAST_LLM=deepseek-chat` → `openai:deepseek-chat` (provider:model 형식 필요)
- **오류 수정 2**: `duckduckgo-search` 패키지 누락 → `Dockerfile.gpt-researcher` 신규 작성으로 해결
  ```dockerfile
  FROM gptresearcher/gpt-researcher:latest
  RUN pip install -U duckduckgo-search --no-cache-dir
  ```
- **현재 상태**: `secondbrain-gpt-researcher` 컨테이너 포트 8888 실행 중

---

### 3. Zettelkasten MCP 등록 (신규)

- **패키지**: `zettelkasten-mcp-1.2.1` (`C:\KJS\tools\zettelkasten-mcp`)
- **`.mcp.json` 생성** (`C:\KJS\AVARTA\secondBrain\.mcp.json`):
  ```json
  {
    "mcpServers": {
      "zettelkasten": {
        "command": "python",
        "args": ["-m", "zettelkasten_mcp.main"],
        "env": {
          "ZETTELKASTEN_NOTES_DIR": "C:/Users/kjswi/Documents/googleDrive/ObsiVault",
          "ZETTELKASTEN_DATABASE_PATH": "C:/KJS/tools/zettelkasten-mcp/data/zettelkasten.db",
          "ZETTELKASTEN_LOG_LEVEL": "WARNING"
        }
      }
    }
  }
  ```
- **활성화 조건**: Claude Code 재시작 필요 (`.mcp.json`은 프로젝트 시작 시 로드됨)

---

### 4. Gemini 채팅 기록 임베딩 완료

- **이전 세션**에서 `parse-gemini-history.py`로 생성한 244개 `04-gemini/` 노트
- **LightRAG 임베딩 확인**: 266개 processed (244 Gemini + 22 기존 노트)
- **상태**: 전체 임베딩 완료, 이제 `/analyze` 스킬로 Gemini 기록 기반 질의 가능

---

### 5. n8n 파이프라인 최종 구성

```
Manual Trigger ─┐
Daily 07:00 KST ─┤
                 ↓
  Pipeline: MiniFlux (starred-only + trafilatura 전문 크롤링)
                 ↓
  Pipeline: ArXiv
                 ↓
  Pipeline: yfinance
                 ↓
  Summarize: DeepSeek V3
                 ↓
  Embed to LightRAG
                 ↓
  Auto-Link Notes (--days-back 1, DeepSeek V3 관계 분석)
                 ↓
  Sync to Notion          ← 이번 세션 추가
                 ↓
  Notify: Linear
```

---

## 현재 서비스 상태

| 서비스 | 포트 | 상태 |
|--------|------|------|
| n8n | 15678 | 실행 중 |
| MiniFlux | 8080 | 실행 중 |
| MiniFlux DB (Postgres) | — | 실행 중 |
| LightRAG | 9621 | 실행 중, 266개 문서 |
| GPT Researcher | 8888 | 실행 중 |

---

## 미완료 / 다음 할 일

### 즉시 확인 필요
- [ ] **GPT Researcher 실제 리서치 테스트**: `http://localhost:8888` 접속 후 한국어 질의 테스트 (DeepSeek + DuckDuckGo 정상 작동 여부)
- [ ] **Zettelkasten MCP 활성화 확인**: Claude Code 재시작 후 `zettelkasten` MCP 서버 연결 여부 확인

### Notion 동기화 개선
- [ ] **02-permanent 노트 동기화**: 현재는 `01-literature`만 파이프라인에 연결됨. `--folder 02-permanent` 별도 실행 또는 파이프라인에 추가 필요
- [ ] **중복 방지 로직 강화**: 현재는 파일명 기준으로만 스킵 처리 — 내용 수정 시 업데이트 미지원 (업데이트 구현 필요)
- [ ] **04-gemini 노트 Notion 동기화 여부 결정**: 244개 Gemini 기록 노트도 Notion에 올릴지 결정

### 자동 연결(auto-linking) 개선
- [ ] **대량 노트 비용 검토**: 244개 Gemini 노트 포함 시 DeepSeek 호출 수 급증 — 비용/시간 측면 검토 후 `LINK_FOLDERS`에서 `04-gemini` 포함 여부 결정
- [ ] **첫 전체 실행**: `--days-back 0` 옵션으로 기존 노트 전체 연결 분석 실행 (시간/비용 감안하여 결정)

### LightRAG 품질 관리
- [ ] **불필요 도메인 필터링**: `yfinance` 노트(주가 데이터)가 그래프에 노이즈로 작용할 수 있음 — 임베딩 폴더 범위 재검토
- [ ] **BBC 오염 재발 방지**: MiniFlux에서 star 기준 필터링으로 개선됐으나, 모니터링 지속

### GPT Researcher 활용
- [ ] **n8n 연동 검토**: 심층 리서치 결과를 자동으로 `02-permanent/` 노트로 저장하는 워크플로우 설계
- [ ] **웹 UI 사용법 정리**: `http://localhost:8888`에서 리포트 생성 → Obsidian 저장 흐름 문서화

### Phase P6 (Quartz 공개) — 선택적
- [ ] **Quartz 설치 및 설정**: Obsidian Vault → 정적 사이트 퍼블리싱
- [ ] **GitHub Actions 배포**: 기존 워크플로우(`.github/workflows/`) 활용

---

## 주요 파일 경로 참조

| 파일 | 경로 |
|------|------|
| 환경변수 | `C:\KJS\AVARTA\secondBrain\.env` |
| Docker Compose | `C:\KJS\AVARTA\secondBrain\docker-compose.yml` |
| MCP 설정 | `C:\KJS\AVARTA\secondBrain\.mcp.json` |
| Notion 동기화 | `C:\KJS\AVARTA\secondBrain\scripts\sync-notion.py` |
| GPT Researcher Dockerfile | `C:\KJS\AVARTA\secondBrain\Dockerfile.gpt-researcher` |
| Obsidian Vault | `C:\Users\kjswi\Documents\googleDrive\ObsiVault` |
| Zettelkasten MCP | `C:\KJS\tools\zettelkasten-mcp` |

---

## 환경변수 현황 (.env)

| 변수 | 상태 |
|------|------|
| `OPENAI_API_KEY` | ✅ 설정됨 |
| `DEEPSEEK_API_KEY` | ✅ 설정됨 |
| `LINEAR_API_KEY` | ✅ 설정됨 |
| `LINEAR_ISSUE_ID` | ✅ `PRI-5` |
| `NOTION_API_KEY` | ✅ 이번 세션 설정 |
| `NOTION_DATABASE_ID` | ✅ `33e2f5d3-8f22-8131-b2df-c0f9c444a1fd` |
| `ANTHROPIC_API_KEY` | ⚠️ 플레이스홀더 (`sk-ant-...`) |
| `GITHUB_TOKEN` | ⚠️ 플레이스홀더 |
